import * as Authentication from "network/auth.network";
import * as PublicJob from "network/public.job.network";
import * as PrivateProfile from "network/private.profile.network";
import * as PrivateJob from "network/private.job.network";
import * as PrivateApplication from "network/private.application";

export const NetworkServices = {
  Authentication,
  PublicJob,
  PrivateProfile,
  PrivateJob,
  PrivateApplication,
};
